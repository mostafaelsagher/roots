# 🧪 دليل الاختبار - منصة جوزور

## ✅ المشاكل التي تم حلها

### 1. صفحة بيضاء فارغة
**المشكلة**: كانت الصفحة تظهر بيضاء عند التشغيل

**الحل**:
- ✅ تم تعديل المسار الافتراضي من `/dashboard` إلى `/auth/login`
- ✅ تم إضافة مجلد assets إلى angular.json
- ✅ الآن التطبيق يبدأ بصفحة تسجيل الدخول مباشرة

### 2. خطأ Zone.js
**المشكلة**: `RuntimeError: NG0908: In this configuration Angular requires Zone.js`

**الحل**:
- ✅ تم إضافة zone.js إلى package.json
- ✅ تم استيراد zone.js في main.ts
- ✅ تم تثبيت المكتبة بنجاح

### 3. ملفات الترجمة
**المشكلة**: ملف ar.json لم يكن متاحاً

**الحل**:
- ✅ تم إضافة src/assets إلى قائمة assets في angular.json
- ✅ الآن ملف الترجمة يُحمّل بشكل صحيح

## 🚀 خطوات التشغيل

### 1. إيقاف السيرفر الحالي
إذا كان السيرفر يعمل، اضغط `Ctrl+C` لإيقافه

### 2. إعادة التشغيل
```bash
npm start
```

### 3. افتح المتصفح
انتقل إلى: `http://localhost:4200`

يجب أن ترى الآن **صفحة تسجيل الدخول** 🎉

## 📝 اختبار المنصة

### تسجيل الدخول
- **البريد الإلكتروني**: أي بريد (مثال: `admin@jozour.com`)
- **كلمة المرور**: أي كلمة مرور (مثال: `123456`)
- **الدور**: اختر أي دور من القائمة

### الأدوار المتاحة
1. **مستورد** - لإنشاء طلبات استيراد
2. **مصدر** - لتقديم عروض أسعار
3. **مزود خدمات لوجستية** - لإدارة الشحن
4. **شركة فحص الجودة** - لفحص المنتجات
5. **مدير النظام** - للوصول الكامل

### بعد تسجيل الدخول

#### كمستورد:
1. اذهب إلى **"الطلبات"** من القائمة الجانبية
2. اضغط **"إنشاء طلب جديد"**
3. املأ النموذج (5 خطوات):
   - معلومات المنتج
   - الكمية
   - معلومات الشحن
   - الخدمات الاختيارية
   - مراجعة وإرسال
4. شاهد الطلب في قائمة الطلبات

#### استكشف المميزات:
- **لوحة التحكم**: إحصائيات ونشاطات
- **عروض الأسعار**: شاهد العروض المتاحة
- **الشحنات**: تتبع الشحنات والمستندات
- **القائمة الجانبية**: تتغير حسب الدور

## 🔍 التحقق من التشغيل الصحيح

### ✅ يجب أن ترى:
- صفحة تسجيل دخول باللغة العربية
- تصميم أخضر احترافي
- نموذج تسجيل دخول مع اختيار الدور
- رسومات توضيحية على اليسار (Desktop)

### ❌ إذا رأيت صفحة بيضاء:
1. افتح Console في المتصفح (F12)
2. ابحث عن أخطاء حمراء
3. تأكد من أن السيرفر يعمل بدون أخطاء
4. جرّب تحديث الصفحة (Ctrl+R)

### 🔧 إذا استمرت المشكلة:
```bash
# أوقف السيرفر (Ctrl+C)

# احذف node_modules وأعد التثبيت
rm -rf node_modules package-lock.json
npm install

# شغّل من جديد
npm start
```

## 📊 ما يجب أن يعمل الآن

✅ صفحة تسجيل الدخول
✅ التوجيه التلقائي
✅ تحميل ملف الترجمة العربية
✅ RTL Layout
✅ الخط العربي (Cairo)
✅ التصميم الكامل

## 🎯 الخطوات التالية

بعد التأكد من أن كل شيء يعمل:

1. جرّب تسجيل الدخول بأدوار مختلفة
2. استكشف جميع الصفحات
3. جرّب إنشاء طلب جديد
4. شاهد البيانات الوهمية
5. اختبر على أحجام شاشات مختلفة

## 💡 نصائح

- استخدم Chrome DevTools للتطوير
- افتح Console لرؤية أي أخطاء
- جرّب Responsive Mode (Ctrl+Shift+M)
- البيانات الوهمية جاهزة للاستخدام

## 🆘 الدعم

إذا واجهت أي مشكلة:
1. تحقق من Console في المتصفح
2. تحقق من Terminal حيث يعمل npm start
3. راجع README.md للمزيد من المعلومات

---

**تم الإصلاح! المنصة جاهزة للاستخدام! 🚀**

